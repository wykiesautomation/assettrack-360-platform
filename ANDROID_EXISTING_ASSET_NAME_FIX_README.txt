AssetTrack 360 Android Existing Asset Name Fix

- /devices/connect?asset_id=<id> now retains and displays the selected asset name.
- An unassigned GENERIC asset selected from its own Asset page is shown as Mobile Tracker compatible.
- Creating the Android registration code converts that same record to TRACKER.
- Asset name, Asset ID and Site are retained.
- No duplicate asset is created.
- TANK and VIBRATION assets are never auto-converted.
- Assets with active devices remain protected and show the existing linked Device UID.
